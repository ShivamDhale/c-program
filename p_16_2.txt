#include <stdio.h>
#include <string.h>

int main()
{
    char arr[]="shivam";
    int len1=strlen(arr);
    printf("string=%s length =%d\n",arr,len1);
    return 0;
}

output:
string=shivam length =6